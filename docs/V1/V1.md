Dashboard route?

Steps:
1.
2. Authorization: Service/Controller layer authorize user for viewing their own resources
   PreAuthorize or pass Auth object to service from controllers, which does checks
   "must own this exact resource” → service / method-level ownership check
   “round must be upcoming” → business rule in service/domain
   “one coupon per round” → service + DB unique constraint"

   Q: How to separate between the concept of a users coupon and a round&model runs attached? is it just a copy of the round but with modified selections?
   I'm thinking of creating Coupon which essentially just is like a round but has selections set and states "Win/Lose/Undetermined"

